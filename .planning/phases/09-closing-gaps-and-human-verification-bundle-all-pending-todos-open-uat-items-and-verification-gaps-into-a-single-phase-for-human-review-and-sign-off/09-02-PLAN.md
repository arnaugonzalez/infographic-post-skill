---
phase: 09-closing-gaps-and-human-verification
plan: 09-02
title: Human verification sign-off for all pending live-API items
goal: Human executor runs all pending live-API tests and confirms sign-off on Phase 6 and Phase 8 open items
type: execute
wave: 2
depends_on: [09-01]
files_modified: []
autonomous: false
requirements: []
must_haves:
  truths:
    - "generate_posts.py runs end-to-end with a live OpenRouter API key and produces two Spanish posts"
    - "Generated posts are publication-ready LinkedIn content with hook line, no placeholders, and distinct technical vs business angles"
    - "generate_posts.py --language ca produces posts entirely in Catalan with no Romance-language drift"
    - "generate_posts.py runs end-to-end in English and exits 0 with linkedin_posts.md written"
    - "generate_pretty.py --codebase runs end-to-end and exits 0 with HTML output written"
  artifacts:
    - path: linkedin_posts.md
      provides: Generated LinkedIn posts from live run
  key_links: []
---

# Plan 09-02: Human verification sign-off

## Objective

All open human-verification items from Phase 6 (06-VERIFICATION.md) and Phase 8 (08-VERIFICATION.md) require a live API key to confirm. This plan is a structured checklist for the human executor to work through in one session.

Purpose: Formally close the `human_needed` status on Phase 8 and the three pending human verification items on Phase 6.
Output: Human confirms pass/fail on each item below. No code changes expected.

**Prerequisites before starting:**
- `.env` file in the repo root with `INFG_OPENROUTER_API_KEY`, `INFG_LLM_MODEL`, and `INFG_API_KEY` set
- Working directory: repo root (`infographic-skill/`)
- Phase 9 Plan 01 already executed (dead-code fix applied, tests pass)

<execution_context>
@$HOME/.claude-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-integration-and-skill-md/08-VERIFICATION.md
@.planning/phases/06-linkedin-post-generator/06-VERIFICATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Check 1: generate_posts.py end-to-end Spanish run (Phase 6, item 1 + Phase 8, item 1)</name>
  <what-built>
generate_posts.py reads the local codebase, calls the OpenRouter LLM twice (technical and business angles), enforces Spanish language in the system prompt, and writes both posts to linkedin_posts.md.
  </what-built>
  <how-to-verify>
Run the following command from the repo root (ensure .env is loaded or export the vars first):

```
python3 scripts/generate_posts.py . --language es
```

Expected behavior:
1. Script prints progress or runs silently — no Python traceback
2. Stdout shows `--- TECHNICAL POST ---` followed by a Spanish-language post
3. Stdout shows `--- BUSINESS POST ---` followed by a distinct Spanish-language post
4. Each post is between 800 and 1,600 characters (you can count roughly — posts should be 3-5 paragraphs)
5. `linkedin_posts.md` is written (or overwritten) in the current directory
6. Script exits 0 (check with `echo $?`)

Confirm each:
- [ ] Exit code is 0
- [ ] `--- TECHNICAL POST ---` separator visible on stdout
- [ ] `--- BUSINESS POST ---` separator visible on stdout
- [ ] Posts are in Spanish (not English)
- [ ] `linkedin_posts.md` exists: `ls -la linkedin_posts.md`
  </how-to-verify>
  <resume-signal>Type "passed" if all 5 items checked. Describe any failures otherwise.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Check 2: Post quality assessment (Phase 6, item 2)</name>
  <what-built>
The two generated Spanish posts from Check 1.
  </what-built>
  <how-to-verify>
Open `linkedin_posts.md` (or read the stdout output from Check 1) and assess both posts:

**Technical post — check for:**
- [ ] First sentence is a hook (not a generic opener like "This project is...")
- [ ] At least one concrete implementation detail (a specific library, pattern, algorithm, or architecture choice)
- [ ] No unfilled placeholder tokens (no `[PLACEHOLDER]`, `{topic}`, `<insert>`, or similar)
- [ ] Reads as publication-ready LinkedIn content

**Business post — check for:**
- [ ] First sentence leads with an outcome or value (not a technical detail)
- [ ] Framing is for a non-technical audience (PM, exec, investor)
- [ ] No unfilled placeholder tokens
- [ ] Reads as publication-ready LinkedIn content

**Structural distinction check:**
- [ ] The two posts are NOT paraphrases of each other — they have meaningfully different angles

Quality is subjective. The bar is "would I post this without editing?" — not perfection.
  </how-to-verify>
  <resume-signal>Type "passed" if the posts meet the bar above, or describe which checks failed.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Check 3: Catalan language drift resistance (Phase 6, item 3)</name>
  <what-built>
The negative language constraint in both system prompts that forbids Spanish, Portuguese, French, and Italian when the output language is Catalan.
  </what-built>
  <how-to-verify>
Run from the repo root:

```
python3 scripts/generate_posts.py . --language ca
```

Expected behavior:
1. Script exits 0
2. Both posts on stdout are entirely in Catalan
3. No Spanish sentences (e.g., no "Este proyecto", "La aplicación", "Los usuarios")
4. No Portuguese sentences (e.g., no "Este projeto", "A aplicação")
5. No French sentences (e.g., no "Ce projet", "L'application")

Catalan identifiers: words like "aquest", "projecte", "codi", "funció", "podem", "usuaris", "desenvolupament".

Confirm:
- [ ] Exit code is 0
- [ ] Technical post is in Catalan throughout
- [ ] Business post is in Catalan throughout
- [ ] No drift into Spanish, Portuguese, or French detected
  </how-to-verify>
  <resume-signal>Type "passed" if both posts are fully in Catalan. Note any drift sentences if failed.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Check 4: generate_posts.py English end-to-end run (Phase 8, item 1 — English variant)</name>
  <what-built>
Same pipeline as Check 1 but in English, confirming the default language path.
  </what-built>
  <how-to-verify>
Run from the repo root:

```
python3 scripts/generate_posts.py . --language en
```

Expected behavior:
1. Exit code 0
2. `--- TECHNICAL POST ---` and `--- BUSINESS POST ---` separators on stdout
3. Posts are in English
4. `linkedin_posts.md` updated

Confirm:
- [ ] Exit code is 0 (`echo $?`)
- [ ] Both separators visible on stdout
- [ ] Posts are in English
- [ ] `linkedin_posts.md` updated (check mtime: `ls -la linkedin_posts.md`)
  </how-to-verify>
  <resume-signal>Type "passed" or describe failure.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Check 5: generate_pretty.py --codebase end-to-end run (Phase 8, item 2)</name>
  <what-built>
The --codebase flag on generate_pretty.py that reads the local codebase via read_codebase.py, selects a prompt strategy from the registry, and generates an infographic HTML file.
  </what-built>
  <how-to-verify>
Run from the repo root (requires `INFG_API_KEY` set in .env — this is the Google AI Studio key, not the OpenRouter key):

```
python3 scripts/generate_pretty.py --codebase . --output /tmp/codebase.html
```

Expected behavior:
1. Script runs (may take 30-60 seconds for the Gemini API call)
2. Exit code 0
3. `/tmp/codebase.html` is written

Confirm:
- [ ] Exit code is 0 (`echo $?`)
- [ ] `/tmp/codebase.html` exists and is non-empty: `ls -lh /tmp/codebase.html`
- [ ] (Optional) Open /tmp/codebase.html in a browser to confirm it renders an infographic

Note: If INFG_API_KEY is not available, mark this check as "skipped — no Google API key" and proceed. The other four checks are the primary sign-off.
  </how-to-verify>
  <resume-signal>Type "passed", "skipped — no Google API key", or describe failure.</resume-signal>
</task>

</tasks>

<verification>
All five checks above are the verification. Phase 9 is complete when:
- Checks 1-4 all report "passed" (live OpenRouter checks)
- Check 5 reports "passed" or "skipped — no Google API key"
</verification>

<success_criteria>
- Phase 6 human_verification items 1, 2, and 3 are formally closed
- Phase 8 human_verification items 1 and 2 are formally closed (or marked skipped with reason)
- v1.1 milestone human sign-off is complete
</success_criteria>

<output>
After completion, create `.planning/phases/09-closing-gaps-and-human-verification-bundle-all-pending-todos-open-uat-items-and-verification-gaps-into-a-single-phase-for-human-review-and-sign-off/09-02-SUMMARY.md`

The summary should record the pass/fail result for each of the 5 checks, note the final v1.1 milestone status, and list any checks that were skipped with reasons.
</output>
