---
phase: 08-integration-and-skill-md
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SKILL.md
autonomous: false
requirements: []

must_haves:
  truths:
    - "User can open SKILL.md and find copy-pasteable invocation examples for read_codebase.py, generate_posts.py, oss_audit.py, and generate_pretty.py --codebase"
    - "Every example command in SKILL.md runs without unrecognized-argument errors"
    - "An end-to-end run of generate_pretty.py --codebase and generate_posts.py both complete successfully on this repo"
    - "New v1.1 commands are grouped together in a single 'Codebase Tools' section per D-01"
    - "v1.0 SKILL.md content is unchanged"
  artifacts:
    - path: "SKILL.md"
      provides: "v1.1 Codebase Tools documentation section"
      contains: "## Codebase Tools"
  key_links:
    - from: "SKILL.md"
      to: "scripts/generate_posts.py"
      via: "documented CLI invocation"
      pattern: "python3 scripts/generate_posts.py"
    - from: "SKILL.md"
      to: "scripts/generate_pretty.py"
      via: "documented --codebase flag"
      pattern: "--codebase"
    - from: "SKILL.md"
      to: "oss_audit.py"
      via: "documented CLI invocation"
      pattern: "python3 oss_audit.py"
    - from: "SKILL.md"
      to: "scripts/read_codebase.py"
      via: "documented utility reference"
      pattern: "read_codebase.py"
---

<objective>
Add a "Codebase Tools" section to SKILL.md documenting all v1.1 capabilities with copy-pasteable examples, then verify every example command runs without errors on this repository.

Purpose: This is the final phase of v1.1. Users need accurate, working documentation for the four new capabilities (codebase reader, LinkedIn posts, codebase infographics, OSS audit). No new code is created -- only documentation and verification.

Output: Updated SKILL.md with "Codebase Tools" section; human-verified that all example commands exit 0.
</objective>

<execution_context>
@$HOME/.claude-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-integration-and-skill-md/08-CONTEXT.md
@SKILL.md
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Codebase Tools section to SKILL.md</name>
  <files>SKILL.md</files>
  <read_first>
    - SKILL.md (current 628-line file; new section appended after line 627)
    - .planning/phases/08-integration-and-skill-md/08-CONTEXT.md (locked decisions D-01 through D-07)
    - .env.example (canonical env var names for Requires lines)
    - scripts/generate_posts.py (verify CLI: `python3 scripts/generate_posts.py --help`)
    - scripts/generate_pretty.py (verify CLI: `python3 scripts/generate_pretty.py --help`, confirm --codebase flag)
    - oss_audit.py (verify CLI: `python3 oss_audit.py --help`)
    - scripts/read_codebase.py (verify CLI: `python3 scripts/read_codebase.py --help`)
  </read_first>
  <action>
Append a new section to SKILL.md after the existing content (after line 627). The section structure must exactly follow this specification per D-01, D-02, D-03, D-04:

```markdown
---

## Codebase Tools

> **v1.1** — These commands read and analyse a local codebase to produce infographics,
> LinkedIn posts, and quality audit reports.
>
> **Requires:** All commands below need `INFG_OPENROUTER_API_KEY` and `INFG_LLM_MODEL`
> set in `.env` (see `.env.example`). The `--codebase` pretty-mode flag additionally
> needs `INFG_API_KEY` (Google AI Studio) for image generation.

### Generate LinkedIn posts from a codebase

Generates two LinkedIn posts (technical angle for engineers + business angle
for PMs/execs) from a codebase directory in a single command. User selects
the output language at runtime.

```bash
python3 scripts/generate_posts.py . --language en
```

Output: two posts printed to stdout (`--- TECHNICAL POST ---` / `--- BUSINESS POST ---`) and saved to `linkedin_posts.md`.
Requires: `INFG_OPENROUTER_API_KEY`, `INFG_LLM_MODEL`

### Generate a pretty infographic from a codebase

Scans a codebase directory, builds a structured summary, and generates a
Gemini-powered architecture infographic — all in one command.

```bash
python3 scripts/generate_pretty.py --codebase . --output codebase.html
```

Output: `codebase.html` (+ auto `.png` via Playwright if installed).
Requires: `INFG_API_KEY` (image generation), `INFG_OPENROUTER_API_KEY` + `INFG_LLM_MODEL` (text/HTML path)

### Run an OSS quality audit

Produces a structured audit report covering test coverage, docstring gaps,
OSS baseline file presence, flake8 logical errors, and complexity hotspots.

```bash
python3 oss_audit.py --root .
```

Output: `QUALITY_AUDIT.md` written to the project root.
Requires: `coverage` and `flake8` installed (`pip install -r requirements-audit.txt`)

### Read a codebase (utility)

Low-level utility used internally by `generate_posts.py` and
`generate_pretty.py --codebase`. Can also be run standalone to inspect the
filtered codebase summary as JSON.

```bash
python3 scripts/read_codebase.py . --budget 40000 -o report.json
```

Output: JSON `CodebaseReport` to stdout (or file with `-o`).
Requires: no API keys (runs locally)
```

Also update the Decision rule table (around line 393-399) by adding three new rows after the existing three rows:

| "codebase infographic" or "pretty from codebase" | `generate_pretty.py --codebase <dir>` |
| "linkedin posts from codebase" | `generate_posts.py <dir> --language en` |
| "audit" or "quality audit" | `oss_audit.py --root <dir>` |

IMPORTANT: Do NOT modify any existing v1.0 content above line 627 except for inserting the new decision-rule table rows. Only append the Codebase Tools section at the end.
  </action>
  <verify>
    <automated>grep -c "## Codebase Tools" SKILL.md | grep -q "1" && grep -c "generate_posts.py" SKILL.md | grep -qv "^0$" && grep -c "oss_audit.py" SKILL.md | grep -qv "^0$" && grep -c "\-\-codebase" SKILL.md | grep -qv "^0$" && grep -c "read_codebase.py" SKILL.md | grep -qv "^0$" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md contains exactly one `## Codebase Tools` header
    - SKILL.md contains `python3 scripts/generate_posts.py . --language en` as a code block example
    - SKILL.md contains `python3 scripts/generate_pretty.py --codebase . --output codebase.html` as a code block example
    - SKILL.md contains `python3 oss_audit.py --root .` as a code block example
    - SKILL.md contains `python3 scripts/read_codebase.py . --budget 40000 -o report.json` as a code block example
    - SKILL.md contains `Requires:` line after each example command
    - SKILL.md contains `INFG_OPENROUTER_API_KEY` in the Codebase Tools section
    - SKILL.md decision rule table contains row with `generate_posts.py`
    - SKILL.md decision rule table contains row with `oss_audit.py`
    - SKILL.md decision rule table contains row with `--codebase`
    - Lines 1-627 of SKILL.md are unchanged from the original (v1.0 content preserved)
    - Total line count of SKILL.md is greater than 627
  </acceptance_criteria>
  <done>SKILL.md contains a "Codebase Tools" section with four subsections (generate_posts.py, generate_pretty.py --codebase, oss_audit.py, read_codebase.py), each with a 1-2 sentence description, one bash example command, an Output line, and a Requires line. Decision rule table updated with three new rows. All v1.0 content unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify SKILL.md example commands run on this repository</name>
  <files>SKILL.md</files>
  <read_first>
    - SKILL.md (the updated file with example commands to verify)
  </read_first>
  <action>
Run each of the four SKILL.md example commands against this repository. The executor should run the two commands that do NOT require API keys automatically (read_codebase.py and oss_audit.py). The two API-dependent commands (generate_posts.py and generate_pretty.py --codebase) should be tested with --help to confirm argument parsing, then the human verifies full execution with real API keys.

Automated verification (executor runs these):
1. `python3 scripts/read_codebase.py . --budget 40000 -o /tmp/report.json` -- must exit 0
2. `python3 oss_audit.py --root .` -- must exit 0 (with audit deps installed)
3. `python3 scripts/generate_posts.py --help` -- must show `directory` and `--language` args without error
4. `python3 scripts/generate_pretty.py --help` -- must show `--codebase` flag without error

Human verification (presented at checkpoint):
5. `python3 scripts/generate_posts.py . --language en` -- with INFG_OPENROUTER_API_KEY set
6. `python3 scripts/generate_pretty.py --codebase . --output /tmp/codebase.html` -- with INFG_API_KEY set
  </action>
  <verify>
    <automated>python3 scripts/read_codebase.py . --budget 40000 -o /tmp/report.json && python3 oss_audit.py --root . && python3 scripts/generate_posts.py --help 2>&1 | grep -q "language" && python3 scripts/generate_pretty.py --help 2>&1 | grep -q "codebase" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `python3 scripts/read_codebase.py . --budget 40000 -o /tmp/report.json` exits 0
    - `python3 oss_audit.py --root .` exits 0 (with audit deps installed)
    - `python3 scripts/generate_posts.py . --language en` exits 0 (with API keys set) OR fails only due to missing API key (not unrecognized arguments)
    - `python3 scripts/generate_pretty.py --codebase . --output /tmp/codebase.html` exits 0 (with API keys set) OR fails only due to missing API key (not unrecognized arguments)
    - No example command produces an `unrecognized arguments` error
  </acceptance_criteria>
  <done>All four SKILL.md example commands verified: read_codebase.py and oss_audit.py exit 0, generate_posts.py and generate_pretty.py --codebase either exit 0 with API keys or fail only on missing credentials. No argument parsing errors.</done>
</task>

</tasks>

<verification>
1. SKILL.md contains a `## Codebase Tools` section with exactly four subsections
2. Each subsection follows the D-04 format: description, bash example, Output line, Requires line
3. The decision rule table has new rows for codebase infographic, LinkedIn posts, and audit
4. All example commands parse without `unrecognized arguments` errors against this repo's scripts
5. v1.0 SKILL.md content (lines 1-627) is preserved exactly
</verification>

<success_criteria>
- SKILL.md "Codebase Tools" section exists with four documented commands
- Every example command in SKILL.md either runs successfully or fails only due to missing API credentials (never due to wrong arguments)
- v1.0 content preserved, no breaking changes
- Human has verified at least the argument-parsing step of each command
</success_criteria>

<output>
After completion, create `.planning/phases/08-integration-and-skill-md/08-01-SUMMARY.md`
</output>
