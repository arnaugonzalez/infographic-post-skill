matplotlib>=3.7
Pillow>=10.0
numpy>=1.24
requests>=2.28
pathspec>=0.12.1
simplepycons>=1.0
jinja2>=3.1

# AI-powered "pretty mode" (Gemini image generation via AI Studio / Vertex)
# Required if INFG_API_KEY or INFG_VERTEX_PROJECT is set; falls back to HTML
# template rendering when missing.
google-genai>=1.0

# Optional: auto-screenshot HTML to PNG (only needed for legacy HTML mode export)
# playwright>=1.40
